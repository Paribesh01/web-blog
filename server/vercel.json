{
  "version": 2,
  "buildCommand": "next build",
  "builds": [
    {
      "src": "./backend/server.js",
      "use": "@vercel/node"
    }
  ],

  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
